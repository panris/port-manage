{
  "name": "port-manage-mcp",
  "version": "1.0.0",
  "description": "MCP server for port-manage: scan ports, search processes, kill processes",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^22.0.0"
  }
}
